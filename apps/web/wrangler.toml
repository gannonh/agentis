name = "agentis-support-agent-preview"
main = "src/worker/support-agent-worker.ts"
compatibility_date = "2026-05-16"
workers_dev = true

[vars]
SUPPORT_AGENT_PROVIDER = "openai"
SUPPORT_AGENT_MODEL = "gpt-5.4-mini"

[env.preview]
name = "agentis-support-agent-preview"
workers_dev = true

[env.preview.vars]
SUPPORT_AGENT_PROVIDER = "openai"
SUPPORT_AGENT_MODEL = "gpt-5.4-mini"
# Public namespace label for browser-safe AI Search status when the binding is enabled.
# SUPPORT_AGENT_AI_SEARCH_NAMESPACE = "agentis-support-agent"

# Cloudflare AI Search namespace binding (M006 / AIS-01).
# Uncomment after creating the namespace in Cloudflare. For local Wrangler dev against
# remote AI Search, add `remote = true`.
# [[ai_search_namespaces]]
# binding = "SUPPORT_AGENT_AI_SEARCH"
# namespace = "agentis-support-agent"

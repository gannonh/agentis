import addTitle from './title.js';
import buildOptions from './build.js';
import initializeClient from './initialize.js';

export { addTitle, buildOptions, initializeClient };

export default {
  addTitle,
  buildOptions,
  initializeClient,
};

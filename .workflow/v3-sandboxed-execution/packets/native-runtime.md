# Native Runtime

Packet ID: native-runtime
Objective: Wire `runWorkspaceCommand` into native tools, policy, payloads, run executor, approvals, aborts, and mock runtime.
Status: completed
Output: API native tool/runtime changes and tests
